interface:
  display_name: "내부 ReportX PDF 렌더링"
  short_description: "이미 준비된 FP3 결과의 호환 렌더링만 수행합니다"
  default_prompt: "Use $render-reportx-fp3-pdf to 이미 준비된 ReportX FP3 결과가 있을 때만 내부 호환 PDF 렌더링을 실행해 줘."
policy:
  allow_implicit_invocation: false
